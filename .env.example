# Base URL of the PredictMind API gateway the web app talks to.
# In local dev the gateway runs on :3001 and proxies to all microservices.
# (Users can also override this at runtime in the Testing page's API URL field.)
NEXT_PUBLIC_API_URL=http://localhost:3001
